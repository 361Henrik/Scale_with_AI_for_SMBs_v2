# GEMINI_API_KEY: Required for the existing Gemini-powered features
# (AIAssistant chat widget + AgentBuilder wizard).
# AI Studio automatically injects this at runtime from user secrets.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# ANTHROPIC_API_KEY: Required for the live agent demos on the Roles tab
# and the qualified booking-flow recommendation. Uses claude-opus-4-6
# with streaming. Get a key at https://console.anthropic.com/.
ANTHROPIC_API_KEY="MY_ANTHROPIC_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"
